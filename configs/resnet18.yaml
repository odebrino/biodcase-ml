extends: baseline.yaml

model:
  name: resnet18
  pretrained: false
  img_size: 224

training:
  seed: 42
  batch_size: 32
  epochs: 20
  lr: 0.0001
  weight_decay: 0.0001
  num_workers: 2
  use_class_weights: true
  early_stopping_patience: 5
  output_dir: outputs/runs
