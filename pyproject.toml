[project]
name = "biodcase-ml"
version = "0.1.0"
description = "Reproducible bioacoustic classification pipeline for BIODCASE-style annotations."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

