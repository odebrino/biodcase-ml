"""Model definitions.

The maintained submission path is KNN-only and lives in ``src.models.knn_pipeline``.
Historical CNN material was moved under ``legacy/cnn/`` to keep the root
submission path unambiguous.
"""
